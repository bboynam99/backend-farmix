{
  "dev": {
    "DOTENV_CONFIG_PATH": "dev/.dev.env",
    "NODE_OPTIONS": "--experimental-modules --es-module-specifier-resolution=node --import specifier-resolution-node/register -r dotenv/config"
  },
  "test": {
    "DOTENV_CONFIG_PATH": "test/.test.env",
    "NODE_OPTIONS": "--experimental-modules --es-module-specifier-resolution=node --import specifier-resolution-node/register -r dotenv/config"
  },
  "stage": {
     "DOTENV_CONFIG_PATH": "dev/.stage.env",
     "NODE_OPTIONS": "--experimental-modules --es-module-specifier-resolution=node --import specifier-resolution-node/register -r dotenv/config"
  }
}
